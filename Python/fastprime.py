import time





def isPrime(num, prime_list):
    prev = 0
    if prime_list:
        for prime_no in prim_list:
            if num % prime_no == 0:
                return false
                prev = prime_no
            if x / prev > 
    return true

start = time.clock()
p_list = [2]

